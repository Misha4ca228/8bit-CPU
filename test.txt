# ================================================
# Программа: Вывод чисел массива (для прямой LD)
# ================================================

start:
    LD 0, array;       # 1-й элемент
    OUT 0;
    LD 0, array2;      # 2-й элемент
    OUT 0;
    LD 0, array3;      # 3-й элемент
    OUT 0;
    LD 0, array4;      # 4-й элемент
    OUT 0;
    LD 0, array5;      # 5-й элемент
    OUT 0;
    LD 0, array6;      # 6-й элемент
    OUT 0;
    LD 0, array7;      # 7-й элемент
    OUT 0;
    LD 0, array8;      # 8-й элемент
    OUT 0;
    LD 0, array9;      # 9-й элемент
    OUT 0;
    LD 0, array10;     # 10-й элемент
    OUT 0;
    HALT;              # стоп

# ================================================
# Данные
# ================================================

array:
    $1;
array2:
    $2;
array3:
    $3;
array4:
    $4;
array5:
    $5;
array6:
    $6;
array7:
    $7;
array8:
    $8;
array9:
    $9;
array10:
    $10;
